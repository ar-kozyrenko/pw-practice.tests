name: Run API Tests

on:
  workflow_dispatch: # Manual trigger button only

jobs:
  api-tests:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium
        
      - name: Run API tests
        run: |
          echo "Running API tests from getArticlesApi.spec.ts"
          npx playwright test tests/TestWithMethods/getArticlesApi.spec.ts
        
      - name: Upload test results
        if: always() # Upload results even if tests fail
        uses: actions/upload-artifact@v3
        with:
          name: playwright-report-${{ github.run_number }}
          path: |
            playwright-report/
            test-results/
          retention-days: 30

      - name: Print test summary
        if: always()
        run: |
          echo "Test Run Completed"
          echo "Check the playwright-report artifact for detailed results"